/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    trailingSlash: true,
    // basePath: '/age-calculator',
    // assetPrefix: './',
};

export default nextConfig;
